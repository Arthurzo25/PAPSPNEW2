@model IEnumerable<PAPSPNEW.Models.Bairro>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeBairro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPontoTuristicoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeBairro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPontoTuristicoNavigation.IdPontoTuristico)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdBairro">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdBairro">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdBairro">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
